import { Skeleton } from '@nextui-org/react';

export default function SidebarLoading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <Skeleton className='flex h-full w-96' />;
}
